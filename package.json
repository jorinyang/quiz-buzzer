{
  "name": "quiz-buzzer",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:push": "cd supabase && npx prisma db push",
    "db:seed": "cd supabase && npx prisma db seed",
    "db:studio": "cd supabase && npx prisma studio",
    "ws:dev": "cd ws-server && pnpm dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0",
    "prettier": "^3.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
